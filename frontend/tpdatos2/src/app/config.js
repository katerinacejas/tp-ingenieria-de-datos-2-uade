// Cambiá esto al conectar con el back real
export const USE_MOCK = true;

// Base URL para el back (podés usar .env con Vite)
export const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:8080';
